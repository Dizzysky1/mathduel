// Deployment configuration.
//
// RELAY_URL: WebSocket origin of the optional relay worker (see relay/README.md),
// e.g. 'wss://mathduel-relay.example.workers.dev'. Leave empty to disable the
// fallback; online play then requires a direct WebRTC path between players.
export const RELAY_URL = '';
